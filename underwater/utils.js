import {tiny, defs} from '../examples/common.js';

const {Vector, Vector3, vec, vec3, vec4, color, Matrix, Mat4, Shape, Shader, Texture, Component} = tiny;

export {tiny, defs, Vector, Vector3, vec, vec3, vec4, color, Matrix, Mat4, Shape, Shader, Texture, Component};
